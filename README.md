# HƯỚNG DẨN SỬ DỤNG VPS-BOT ĐỂ CHIẾM ĐIỀU KHIỂN

### Linux Debian Based ( Ubuntu,Kali Linux, ...)
 * Bước 1: tải gói cần thiết
    ```bash
    apt update && apt install python3 python3-pip git
    ``` 
